# Cashfree sandbox credentials — never commit the real .env
# Get from: dashboard.cashfree.com > Payment Gateway > Developers > API Keys (Test mode)
CASHFREE_APP_ID=your-cashfree-app-id
CASHFREE_SECRET_KEY=your-cashfree-secret-key

# Port the backend listens on (default: 8080)
PORT=8080
